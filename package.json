{
  "name": "ai-script-generator",
  "version": "1.0.0",
  "description": "AI Presentation Script Generator",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && npx esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.3"
  }
}